debug: false # log? ?? ?? ???? false
management.endpoint.web.exposure.include: "*" # spring actuator? ?????? ??? ??? ?? ??? ???
# actuator : ?? ?? application? ??? ? ? ????, ????? application? ?? ??? ??? ? ?? ??
# ? application? ??, ???? ?? ??, ???? ???, ??? ?????? ?? ?? ?? ?

logging:
  level:
    org.dry: debug # ? ??? ???? ???? ???? debug ??? ???
    org.springframework.web.servlet: debug # response, request ??? debug? ??? ? ??
    org.hibernate.type.descriptor.sql.BasicBinder: trace # jpa ?? ? ?? ??? ? ? ??? ??? ?? ????? ????? ??? ?? ??? trace? ?? ?? ????? ? ? ??

mybatis:
  type-aliases-package: org.dry.entity
#  mapper-locations: classpath:mappers/org/dry/mapper/**/*.xml # why this code works only??
  mapper-locations: classpath:mappers/**/*.xml # this code dont works

spring:
  datasource: # db ?? ??
    url: jdbc:mysql://localhost:3306/dryTable
    username: "un"
    password: "0000"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    defer-datasource-initialization: true # data.sql? ??? ? ???. Test? db? ??? ? ??? ????? ?????? ?? ???? ???? ? ??.
    hibernate.ddl-auto: create # test? ??? ? entity? ?? ???? table? ??? ?
    show-sql: true # sql ?? debug?? ?? ??
    properties:
      hibernate.format_sql: true # ?? ???? ????? ?? ???? ???? ?? ????? ?? ??? ?? ??? ???
      # hibernate?? ???? ? ? ??? ????? ????? ???? ?? ??? ? ???? ??? ??? ? ???
      hibernate.default_batch_fetch_size: 100 # Jpa?? ??? ????? ?????? ??? ???? ??? ??? select? ? ? ?? ??? ??
  sql.init.mode: always # data.sql? ?? ???? ???? ?? ? (NEVER : ????, EMBEDDED : Test ????? ??, ALWAYS : ?? ?????)
  data: # rest test? hal?? ???, ?? url? localhost:8080/api??, @RepositoryRestResource ?????? ?????? ??????.
    rest:
      base-path: /api
      detection-strategy: annotated


--- # yaml ??? ---? ??? ???? document? ?? ? ??
# spring:
# config.activate.on-profile: testdb # testdb profile? activate ?? ? ??? ???
#  datasource:
#    url: jdbc:h2:mem:board;mode=mysql
#    driverClassName: org.h2.Driver
#  sql.init.mode: always
#  test.database.replace: none
