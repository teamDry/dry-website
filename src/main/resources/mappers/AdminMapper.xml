<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.dry.mapper.AdminMapper">
    <!--    SELECT    -->
    <select id="selectByIdAndPassword" parameterType="org.dry.vo.MemberIdAndPassword" resultType="org.dry.entity.Admin">
        SELECT * FROM admin WHERE id = #{id} AND password = #{password}
    </select>

    <!--    INSERT    -->

    <!--    UPDATE    -->

    <!--    DELETE    -->
</mapper>