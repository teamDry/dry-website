<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.dry.mapper.MemberMapper">
    <select id="selectMemberByIdAndPassword" parameterType="org.dry.mapper.MemberIdAndPassword" resultType="org.dry.entity.Member">
        SELECT * FROM member WHERE id = #{id} AND password = #{password}
    </select>

    <insert id="insertMember" parameterType="Member">
        insert into member (id, password, nickname, email, last_entered_at, created_at) values (#{id}, #{password}, #{nickname}, #{email}, null, '2022-12-03 14:02:08');
    </insert>
</mapper>